(function() {
  "use strict";
  // ここに「ブロックスコープ」の説明を記述する
  //const let で変数宣言を行う.{}で囲まれている部分
  //ブロックの中の変数はブロックの外からアクセスすることはできない
  //ブロックの中から、外の変数にアクセスすることはできる

  //「関数スコープ」
  //var を使って宣言した場合は関数スコープになる
  //ifやfor文のブロック内に書かれている変数にもブロック外からアクセスできる
  //関数の引数や関数内で定義されている変数にブロック外からアクセスできない
})();
